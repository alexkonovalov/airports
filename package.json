{
  "name": "airport-routes",
  "version": "1.0.0",
  "description": "Finds the shortest route between airports",
  "author": "Alexander Konovalov",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/shared-constants",
    "packages/*"
  ],
  "scripts": {
    "install-shared": "npm install --prefix packages/shared-constants",
    "build-shared": "npm run build --prefix packages/shared-constants",
    "install-all": "npm install --prefix packages/build-graph && npm install --prefix packages/server",
    "build-all": "npm run build --prefix packages/build-graph && npm run build --prefix packages/server"
  }
}
